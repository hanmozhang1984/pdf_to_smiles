# Core dependencies with pinned versions
PySide6==6.10.1
pypdfium2==5.3.0
pdfplumber==0.11.8
rdkit==2025.9.2
opencv-python==4.9.0.80
Pillow==11.3.0
pandas==2.3.3
pytesseract==0.3.13
requests==2.32.5

# Cloud deployment (only needed if deploying your own Modal backend)
# modal==0.66.0

# =============================================================================
# LIGHTWEIGHT LOCAL INFERENCE (recommended)
# Fast CPU inference using MolScribe + OpenCV (no TensorFlow needed):
# =============================================================================
# molscribe>=1.1.1
# torch>=2.0.0
# albumentations==1.1.0
# timm==0.4.12
# OpenNMT-py==2.2.0

# =============================================================================
# DECIMER LOCAL INFERENCE (optional - heavier, requires TensorFlow)
# Uncomment the following lines to enable DECIMER-based local inference:
# =============================================================================
# decimer==2.7.0
# decimer-segmentation==1.4.0
# tensorflow==2.12.0
# numpy==1.23.5
